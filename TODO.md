# TODO

- 旅行画面の既存の予定にさらに場所と出発時間を入れてプランを追加する
  例:シアトルからシカゴまでのプランを作成済み。そこにシアトルまで帰るプランを追加する
- 目的地を地図上から選択可能に
- Google Maps の共有: 「旅ログを開く」の回避策が実機で効くかの確認(効かなければ Extension の文言だけに倒す)。Android 由来の `g_st=aw` リンク(`/maps/place/<名前>/data=…ftid` で住所も座標も無い)は座標が取れないので、座標未設定で追加してあとからリンクを貼り直す
- プランの１日目など横に日付を表示（例:Aug 25）